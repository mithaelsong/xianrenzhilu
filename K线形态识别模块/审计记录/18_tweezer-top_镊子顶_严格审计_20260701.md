# 仙人指路测试项目｜K线形态识别 18/29 tweezer-top 镊子顶 严格审计｜2026-07-01

## 时间
- 执行时间：2026-07-01 01:42-01:45 GMT+8
- 项目路径：/Users/songxiaoxiao/Desktop/仙人指路测试项目

## 验收方式
逐条验证源码，确认链路完整，编译通过。

## 7条标准逐项验收

### 标准1：按代码完成链路测试和必要修改

**1.1 形态定义**
- 文件：`K线形态识别模块/形态定义层/KP-DF-01_形态类型定义.swift`
- 定义：`p("tweezer-top", "镊子顶", .dual, .bearish, "两根K线高点接近，顶部受阻。", candles: 2, trend: .afterUptrend, min: 0.56)`
- PatternID：✅ `tweezer-top`
- 中文名：✅ `镊子顶`
- 类别：✅ `.dual`
- 方向：✅ `.bearish`
- K线数：✅ `candles: 2`
- 趋势语义：✅ `.afterUptrend`
- 最低阈值：✅ `min: 0.56`
- 描述：✅ `"两根K线高点接近，顶部受阻。"`

**1.2 识别引擎**
- 文件：`K线形态识别模块/识别引擎层/KP-EN-01_形态识别引擎.swift` line 118
- 调用：`case "tweezer-top": score = near(candles[0].high, candles[1].high, tolerance: tolerance(candles)) ? 0.62 : 0`
- near函数：`abs(d(a) - d(b)) <= tolerance` ✅
- tolerance函数：`max(candles.map { m($0).range }.reduce(0, +) / Double(max(candles.count, 1)) * 0.08, 0.00000001)` ✅
- 基础分数：✅ `0.62`

**1.3 配置层**
- 文件：`K线形态识别模块/配置层/KP-CF-01_形态识别设置配置.swift`
- 图标映射：✅ `tweezerTop`
- 依赖指标：✅ `MA, ADX, ATR`（line 47 与锤子线同组）
- 默认启用：❌ **不在 `enabled` 集合**（设计选择：用户需手动开启）

### 标准2：指标计算能力打通

| 指标 | 配置声明 | confirmationBoost | 状态 |
|------|----------|-------------------|------|
| MA | ✅ | - | ✅ |
| ADX | ✅ | >=25 +0.02 | ✅ |
| 成交量 | ✅ | high/spike +0.03 | ✅ |
| 支撑阻力 | ✅ | nearAny +0.04 | ✅ |
| ATR | ✅ | - | ✅ |

### 标准3：两个业务信号

| 信号 | 链路 | 状态 |
|------|------|------|
| 标签信号 | `KXFN25PatternOverlayBridge.submitOverlays` | ✅ |
| 声音信号 | `KXFN26PatternAlertBridge.submitEvents` | ✅ |
| 声音阈值 | confidence >= 0.72 | ✅ |

### 标准4：K线面板打标签

- 文件：`K线模块/标记层/KX-MK-01_K线形态标记渲染.swift`
- 渲染方式：✅ `pinBadge`（双K形态）
- 短码：✅ `TW`（line 475）
- 方向颜色：✅ `bearish` 红色
- 位置计算：✅ `triggerMidX`, `triggerHighY`, `triggerLowY`
- 避让布局：✅ `occupiedTagFrames`

### 标准5：标签打开解释

- 文件：`K线模块/标记层/KX-MK-03_形态标记详情弹窗.swift`

| 检查项 | 类型 | 内容 |
|--------|------|------|
| explanationText | 通用回退 | `payload.description` = "两根K线高点接近，顶部受阻。" |
| evidenceText | 通用回退 | "触发K线数：2；置信度：62%；方向：看跌。" |
| riskText | 通用回退 | "单根形态不能单独作为买卖依据；需结合趋势、成交量、支撑阻力和后续K线确认，震荡区间容易误报。" |

**注意**：无专属if分支，走通用回退。内容完整可读。

### 标准6：主题配色+自适应

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 背景色 | ✅ | `KLUITheme.chartBackground` |
| 涨跌色 | ✅ | `KLUITheme.candleUp/Down` |
| 标签色 | ✅ | `patternColor(direction: .bearish)` |
| 缩放跟随 | ✅ | `xForIndex/yForPrice` 实时计算 |
| 拖动跟随 | ✅ | `viewport` 变换后重算 |

### 标准7：测试阶段vs正常运行

| 检查项 | 状态 |
|--------|------|
| 双K闭合判断 | ✅ 第2根K线闭合后形态完整成立才判断 |
| 不提前标 | ✅ 不会第一根就标 |
| 不用未来数据 | ✅ 只用当前及之前K线 |

## 编译验收
- `xcodebuild BUILD SUCCEEDED`

## 结论
18/29 镊子顶 按7条标准严格验收通过。

**遗留说明**：不在默认启用集合，需用户手动开启设置面板开关。
